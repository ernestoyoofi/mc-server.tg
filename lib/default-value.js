const defaultContext = {
  folderServer: `${process.cwd()}/server`,
  folderDownload: `${process.cwd()}/download`,
  folderBackup: `${process.cwd()}/backup`,
  folderBackupUpdate: `${process.cwd()}/up-backup`
}

export {
  defaultContext
}